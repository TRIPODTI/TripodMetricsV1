<%= form_for(@rate) do |f| %>
  <% if @rate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rate.errors.count, "error") %> prohibited this rate from being saved:</h2>

      <ul>
      <% @rate.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :device_id %><br />
    <%= f.text_field :device_id %>
  </div>
  <div class="field">
    <%= f.label :in_date %><br />
    <%= f.datetime_select :in_date %>
  </div>
  <div class="field">
    <%= f.label :selected_option %><br />
    <%= f.text_field :selected_option %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
